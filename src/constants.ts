import { NumPlayers } from "./enums";

import iconsArr from "./components/icons/iconsArr";

export const NUM_PLAYERS_BUTTONS = [
  NumPlayers.One,
  NumPlayers.Two,
  NumPlayers.Three,
  NumPlayers.Four,
];

export const GRID_SMALL = 8;
export const GRID_BIG = 18;

export const NUM_ICONS = iconsArr.length;

export const RANDOM_NUMS_LIMIT = 100
